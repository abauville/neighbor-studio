<div class="studio-card shadow-lg m-2" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path(studio.photo.key, crop: :fill, width: 420, height: 280) %>);">
  <%= link_to studio_path(studio), class: "d-flex px-4 py-2 w-100" do %>
    <div class="d-flex flex-column justify-content-between w-100 text-white">
      <h3 class="mt-2"><%= studio.name %></h3>
      <div class="d-flex justify-content-between studio-info p-3">
        <div class="studio-info-left">
          <p class="mb-0 text-truncate">Location: <%= studio.address %></p>
          <p class="mb-0">Price: <%= studio.price %>¥/hr</p>
        </div>
        <div class="w-50 d-flex justify-content-end">
        <div class="studio-info-btn py-2 px-2">
          <p class="mb-0">More Info</p>
        </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
