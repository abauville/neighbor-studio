<h1>Bookings#index</h1>
<p>Find me in app/views/bookings/index.html.erb</p>

<ul>
  <% @bookings.each do |booking| %>
    <li><%= link_to "#{booking.start_date} - #{booking.end_date} at #{booking.studio.name}", studio_path(booking.studio) %></li>
  <% end %>
</ul>
